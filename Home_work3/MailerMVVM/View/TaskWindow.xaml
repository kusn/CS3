<Window x:Class="MailerMVVM.View.TaskWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MailerMVVM"
        mc:Ignorable="d"
        Title="TaskWindow" x:Name="taskWindow" Height="425" Width="375">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="100*"/>
            <RowDefinition Height="8*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Margin="2,2,2,2" FontSize="16" TextWrapping="Wrap">
            <TextBlock.Text>
                2. Начать переделывать свое приложение с использованием шаблона MVVM.
В идеале, вся логика должна быть вынесена из класса с пользовательским интерфейсом в модель.
            </TextBlock.Text>
        </TextBlock>
        <Button x:Name="btOk" Content="OK" Grid.Row="1" Click="btOk_Click"/>
    </Grid>
</Window>
